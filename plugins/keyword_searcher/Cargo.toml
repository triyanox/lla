[package]
name = "keyword_searcher"
description = "Searches for specific keywords within text-based files"
version = "0.1.0"
edition = "2021"

[dependencies]
colored = "2.0.0"
serde_json = "1.0.68"
lla_plugin_interface = { path = "../../lla_plugin_interface" }

[lib]
crate-type = ["cdylib"]
